# AI Shell memory and cache (user-specific, not for repo)
.cache/ai-shell/
*.jsonl

# API key (never commit this)
.config/ai-shell/

# Claude/VS Code local settings
.claude/

# OS junk
.DS_Store
Thumbs.db
